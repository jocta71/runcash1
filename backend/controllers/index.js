/**
 * Exporta todos os controladores em um único objeto
 * Facilita a importação em outros arquivos
 */

const rouletteController = require('./rouletteController');

module.exports = {
  rouletteController
}; 