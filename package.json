{
  "name": "runcash",
  "version": "1.0.0",
  "private": true,
  "description": "RunCash Application",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run preview"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}
